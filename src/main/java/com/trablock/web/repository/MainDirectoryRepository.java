package com.trablock.web.repository;

import com.trablock.web.entity.plan.MainDirectory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MainDirectoryRepository extends JpaRepository<MainDirectory, Long> {
}
